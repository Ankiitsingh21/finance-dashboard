# PostgreSQL Database URL
DATABASE_URL="postgresql://username:password@localhost:5432/finance_dashboard?schema=public"

# JWT Secret for token signing (use a strong random string in production)
JWT_SECRET="your-super-secret-jwt-key-change-in-production"

# API URL (optional, defaults to empty for relative URLs)
NEXT_PUBLIC_API_URL=""
